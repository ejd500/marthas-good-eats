{
  "name": "fullstack-javascript-qap3",
  "version": "1.0.0",
  "description": "Build a simple user interface working with server routes and a RESTful API against a single table for a PostgreSQL database. Have node.js scripts for both UI &amp; API access the same DAL that retrieves, creates, updates and deletes data from the chosen database table.",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.9",
    "express": "^4.18.3",
    "express-session": "^1.18.0",
    "jest": "^29.7.0",
    "method-override": "^3.0.0",
    "mongodb": "^6.5.0",
    "pg": "^8.11.3"
  }
}
